<h1>POSTS PAGE</h1>


<%= link_to "Create a Post!", new_post_path %>

<ul>
  <% @posts.each do |p| %>
    <li><h3><strong><%=link_to "#{p.title}", post_path(p) %></strong></h3>
    <p><i>Post by: <%= p.user.user_name %></i></p>
    <p><%= p.body %></p>
    <% end %>
</ul>
