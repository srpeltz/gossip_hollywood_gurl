<h1>POSTS PAGE</h1>


<%= link_to "Create a Post!", new_post_path %>

<ul>
  <% @posts.each do |p| %>
    <li><%= p.title %>
    <br>
    Post by: <%= p.user.user_name %>
    <br>
    <%= p.body %>
    <br>
    <% end %>
</ul>
